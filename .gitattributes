# Keep line endings normalised so files written on Windows stay valid on Linux
# builds. SQL in particular must never gain a BOM or CRLF — the Prisma migration
# engine rejects both.
* text=auto eol=lf
*.sql text eol=lf working-tree-encoding=UTF-8
